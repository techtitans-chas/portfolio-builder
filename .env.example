# Frontend
NUXT_PUBLIC_API_URL=http://localhost:3111

# Backend
PORT=3111

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=portfolio_builder

# Database URL (local non-Docker: use localhost; Docker: set in docker-compose.yml)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/portfolio_builder
